# Parcel data into usable format

def load_dataset(tier, num_samples=-1):
    # Subsample from frames (eventually sample disproportionately from action frames)
    # Compress
    # Turn into 3 channel array # from scipy import ndimage (.mread)
    # State is three frames
    # Partition randomly into train, test, etc to reduce time dependency
    return []
